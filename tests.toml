#:schema https://raw.githubusercontent.com/YunoHost/apps/master/schemas/tests.v1.schema.json

test_format = 1.0

[default]

    # ------------
    # Tests to run
    # ------------

    preinstall = """
    sudo yunohost tools update apps
    sudo yunohost app install mlmmj -a "domain=domain.tld&path=&init_main_permission=all_users&language=en&list_email=list@domain.tld&owner=package_checker" --force
    """
